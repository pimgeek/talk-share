



  <div class="panel-body">

      <!-- List group -->

      <ul class="list-group">


          <li class="list-group-item">
              <div class="row">
                <h4><a href="/topics/<%= @topic.id %>"><%= @topic.title1 %></a></h4>
                <P>
                  <ul>
                    <li>
                      <%= @topic.title1_tag %><%= @topic.title1 %>
                    </li>
                    <li>
                      <%= @topic.title2_tag %><%= @topic.title2 %>
                    </li>
                    <% @topic.topic_fields.each do |f| %>
                    <li>
                      <%= f.title_tag %><%= f.title %>
                    </li>
                    <% end %>
                  </ul>
                  
                </p>

                <p class="time"><%= time_ago_in_words(@topic.created_at) %>前</p>

              </div>
          </li>

          
<%= link_to t('.back', :default => t("helpers.links.back")),
              topics_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_topic_path(@topic), :class => 'btn btn-default' %>
<%#= link_to t('.destroy', :default => t("helpers.links.destroy")),
              topic_path(@topic),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>

      </ul>


  </div>
